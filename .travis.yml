language: rust
rust:
  - stable
  - beta
  - nightly
notifications:
  email:
    - adolphc@outlook.com
script:
    - cargo build --verbose
    - cargo test --verbose
    - cargo build --example imgui_demo # make sure the example can be compiled.